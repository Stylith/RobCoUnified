[package]
name = "robcos-native-services"
version = "0.3.2"
edition = "2021"
license = "MIT"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
dirs = "5"
robcos = { path = "../.." }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
