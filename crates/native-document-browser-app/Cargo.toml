[package]
name = "nucleon-native-document-browser-app"
version = "0.4.4"
edition = "2021"
license = "GPL-3.0-only"

[dependencies]
nucleon-native-file-manager-app = { path = "../native-file-manager-app" }
