[package]
name = "robcos-native-tweaks-app"
version = "0.4.4"
edition = "2021"
license = "GPL-3.0-only"

[dependencies]
robcos = { path = "../.." }
eframe = { version = "0.29", default-features = false }
