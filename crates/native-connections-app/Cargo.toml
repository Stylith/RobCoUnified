[package]
name = "nucleon-native-connections-app"
version = "0.4.4"
edition = "2021"
license = "GPL-3.0-only"

[dependencies]
nucleon-native-services = { path = "../native-services" }
nucleon-shared = { path = "../shared" }
