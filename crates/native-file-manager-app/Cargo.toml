[package]
name = "robcos-native-file-manager-app"
version = "0.3.2"
edition = "2021"
license = "GPL-3.0-only"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
anyhow = "1"
robcos-native-services = { path = "../native-services" }
robcos-shared = { path = "../shared" }
