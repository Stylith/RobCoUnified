RobCoOS 0.3.2 highlights since 0.3.1:

- switch macOS releases to honest CLI-only bundles instead of a Finder `.app` wrapper
- remove the unused macOS app-bundle icon and plist packaging files
- keep Linux x86_64, Linux ARM, and Windows release packaging unchanged
